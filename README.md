# POD Multi-Agent System

AI 驱动的 Print-on-Demand 自动化工作流系统，基于 LangGraph 多智能体架构。

## 🎯 项目简介

本项目实现了一个端到端的 POD (Print-on-Demand) 自动化系统，通过多个 AI Agent 协同工作，完成从趋势分析到设计生成、质量检查、Mockup 创建直至平台上架的完整流程。

## ✨ 核心功能

| Agent | 职责 | 技术 |
|-------|------|------|
| **Trend Analysis** | 分析市场趋势，生成设计提示词 | Claude 3.5 Sonnet |
| **Design Generation** | 根据提示词生成设计图 | DALL-E 3 |
| **Quality Check** | 质量评估，支持自动重试 | Claude 3.5 Sonnet |
| **Mockup Creation** | 创建产品 Mockup | Printful API |
| **SEO Optimization** | 优化商品标题和描述 | Claude 3.5 Sonnet |
| **Platform Upload** | 上传至电商平台 | Etsy/Printful API |

## 🏗️ 技术架构

```
┌─────────────────────────────────────────────────────┐
│                    Frontend (Next.js)               │
│         Dashboard / Campaign / Designs / Status     │
└─────────────────────────┬───────────────────────────┘
                          │ HTTP/REST
┌─────────────────────────▼───────────────────────────┐
│                  Backend (FastAPI)                  │
│                   /api/v1/*                         │
└─────────────────────────┬───────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────┐
│              LangGraph Workflow Engine              │
│  TrendAnalysis → DesignGen → QualityCheck → ...    │
└─────────────────────────────────────────────────────┘
```

## 🚀 快速开始

### 1. 克隆项目

```bash
git clone <repo-url>
cd pod_multi_agent_system
```

### 2. 配置环境变量

```bash
cd backend
cp .env.example .env
# 编辑 .env，配置 YUNWU_API_KEY
```

### 3. Docker 部署 (推荐)

```bash
docker-compose up -d --build
```

### 4. 本地开发

**后端：**
```bash
cd backend
pip install -r requirements.txt
python main.py
```

**前端：**
```bash
cd frontend
npm install
npm run dev
```

## 📡 API 端点

| 端点 | 方法 | 描述 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/api/v1/workflows` | POST | 创建工作流 |
| `/api/v1/workflows/{id}` | GET | 查询工作流状态 |
| `/api/v1/designs` | GET | 获取设计列表 |
| `/api/v1/listings` | GET | 获取商品列表 |

API 文档：`http://localhost:8000/docs`

## 🔧 环境变量

| 变量 | 必需 | 描述 |
|------|------|------|
| `YUNWU_API_KEY` | ✅ | Yunwu.ai 中转 API Key |
| `YUNWU_API_BASE` | ❌ | API Base URL (默认: https://yunwu.ai/v1) |
| `DEMO_MODE` | ❌ | 演示模式，跳过实际上传 (默认: true) |
| `PRINTFUL_API_KEY` | ❌ | Printful API Key |
| `ETSY_API_KEY` | ❌ | Etsy API Key |

## 📁 项目结构

```
pod_multi_agent_system/
├── backend/
│   ├── agents/           # AI Agent 实现
│   ├── api/              # FastAPI 路由
│   ├── config/           # 配置管理
│   ├── core/             # 核心组件 (State, Workflow)
│   ├── main.py           # 应用入口
│   ├── Dockerfile
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── app/          # Next.js 页面
│   │   ├── components/   # UI 组件
│   │   └── services/     # API 客户端
│   └── package.json
└── docker-compose.yml
```

## 📝 License

MIT License